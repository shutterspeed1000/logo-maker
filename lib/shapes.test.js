const logoShape = require("./shapes.js");
const triangle = require("./shapes.js");
const data = require("./shapes.js");



describe("Triangle", () => {
  test("should render svg for a blue polygon element", () => {

describe('shapeMaker', () =>{
  it(`should send values to select the correct class and supply logo info`), () => {
    const expectedSvg =
    '<polygon points="150, 18 244, 182 56, 182" fill="bisque" />';
    const data = {logoTxt: 'TST', txtColor: 'Red', logoShape: 'Circle', shapeColor: 'Blue'}
 
  const actualSvg = logoInfo.shapeRet();
  expect(actualSvg).toEqual(expectedSvg);

  }

}
)


  });
});

